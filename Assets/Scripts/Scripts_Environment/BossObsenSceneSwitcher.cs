using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.SceneManagement;

public class BossObsenSceneSwitcher : MonoBehaviour
{
  
/*
    // Update is called once per frame
    void Update()
    {

        if (bossAiObsidian.instance.bossHealth <= 0)
        {
            Debug.LogError("The fuck is this");
            BossObsenDeath();

        }

    }
    void BossObsenDeath()
    {
        SceneManager.LoadScene("YB_MainMenu", LoadSceneMode.Single);
    }
*/

}
